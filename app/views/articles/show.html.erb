<%= render "shared/header2" %>
<div class="topver"></div>
<%# 記事の概要 %>
<div class="top-links">
  <a href="/" class="top-btn">
    top ＞
  </a>
  <a href="#" class="category-btn"><%= @article.category.name %></a>
</div>

<div class="article-title">
  <h2 class="article-name">
    <%= @article.article_name %>
  </h2>
  <div class="article-navi">
    <% if user_signed_in? && current_user.id == @article.user_id %>
      <%= link_to "編集", edit_article_path(@article.id), method: :get, class: "article-edit-btn" %>
      <%= link_to "削除", article_path(@article.id), method: :delete, class:"article-destroy-btn" %>
    <% end %>
  </div>
</div>
<div class="article-show">
  <div class="article-img-box">
    <div class="item-img-content">
      <%= image_tag @article.image ,class:"item-box-img" %>
    </div>
  </div>
  <div class="article-info-box">
    <table class="detail-table">
      <tbody>
        <tr>
          <th class="detail-item">投稿者</th>
          <td class="detail-value"><%= link_to  @article.user.username, "/users/#{current_user.id}" %></td>
        </tr>
        <tr>
          <th class="detail-item">カテゴリー</th>
          <td class="detail-value"><%= @article.category.name %></td>
        </tr>
      </tbody>
    </table>
    <div class="item-explain-box">
      <span><%= @article.text %></span>
    </div>
 
  </div>
</div>
  <%# /商品の概要 %>

  <%#div class="comment-box">
    <form>
      <textarea class="comment-text"></textarea>
      <p class="comment-warn">
        相手のことを考え丁寧なコメントを心がけましょう。
        <br>
        不快な言葉遣いなどは利用制限や退会処分となることがあります。
      </p>
      <button type="submit" class="comment-btn">
        <%#= image_tag "comment.png" ,class:"comment-flag-icon" ,width:"20",height:"25"%>
        <%#span>コメントする<span>
      </button>
    </form>
  </div%>

<%= render "shared/second-footer"%>
